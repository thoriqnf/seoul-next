const names: string = "revou";
const myName = "Thoriq";

const frameworks: (string | number)[] = ["next", "nest", "express", "react", 0];
const library: Array<string | number> = ["next", "nest", "express", "react", 0];

function formatPrice(price: number): void {
  // if (price === 0) {
  //   return "harganya murah";
  // }
  // return "harganya ga ada";
  // return 0
}
